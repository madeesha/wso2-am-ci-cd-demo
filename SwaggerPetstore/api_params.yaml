environments:
  - name: test
    endpoints:
      production:
        url: 'https://test.wso2.com'
        config:
          retryTimeOut: $RETRY
  - name: staging
    endpoints:
      production:
        url: 'https://staging.wso2.com'
        config:
          retryTimeOut: $RETRY
  - name: prod
    endpoints:
      production:
        url: 'https://prod.wso2.com'
        config:
          retryTimeOut: $RETRY